# No secrets are required for the local demo.
# Production identity and D1 bindings are injected by Sites.

# Optional observability integrations. Never include prayer-request text.
NEXT_PUBLIC_POSTHOG_KEY=
SENTRY_DSN=

# Comma-separated administrator accounts. Configure this only in Sites runtime settings.
DUODOSH_ADMIN_EMAILS=
